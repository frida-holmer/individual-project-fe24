export interface Genre {
    id: number;
    name: string;
    slug: string;
}

export interface GenreDetails {
    id: number;
    name: string;
    slug: string;
    image_background: string;
    description: string;
}
